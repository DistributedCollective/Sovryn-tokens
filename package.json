{
  "name": "nft-test",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "truffle migrate --network rskTest  --skipDryRun",
    "migrateropsten": "truffle migrate --reset  --network ropsten  --skipDryRun"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "openzeppelin-solidity": "3.2.0",
    "@truffle/hdwallet-provider": "^1.1.1"
  }
}
